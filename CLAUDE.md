# Agent Harness

Claude Code should follow the shared project protocol in `AGENT_HARNESS.md`.

Use request-scoped files under `agent-runs/<request-id>/` and do not mark work complete without verification evidence.
